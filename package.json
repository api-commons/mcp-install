{
  "name": "mcp-install",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The universal install button for MCP servers — one button, every client. A hosted chooser, an embeddable web component, badge generation, and an open machine-readable registry of how every MCP client installs a server.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.widget.config.ts",
    "preview": "vite preview"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^5.4.11"
  }
}
